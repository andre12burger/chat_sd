# Backend - Chat Engine
# (sem dependências externas — usa apenas stdlib)

# Web Gateway
Flask==2.3.3
Flask-SocketIO==5.3.4
python-socketio==5.9.0
python-engineio==4.7.1

# ASGI/Worker para produção (necessário no Render.com)
eventlet==0.33.3

# Desenvolvimento e Debug
python-dotenv==1.0.0
